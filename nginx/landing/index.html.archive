<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="FlotteQ — Plateforme de gestion de flotte automobile" />
  <title>FlotteQ</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }

    :root {
      --bg: #0b1220;
      --bg-2: #131a2b;
      --primary: #3b82f6;
      --primary-hover: #2563eb;
      --text: #f8fafc;
      --text-dim: #94a3b8;
      --border: rgba(148, 163, 184, 0.15);
      --card-bg: rgba(255, 255, 255, 0.04);
      --card-bg-hover: rgba(255, 255, 255, 0.08);
    }

    html, body { height: 100%; }

    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Inter, Roboto, sans-serif;
      background: radial-gradient(1200px 600px at 50% -20%, rgba(59, 130, 246, 0.15), transparent),
                  linear-gradient(180deg, var(--bg) 0%, var(--bg-2) 100%);
      color: var(--text);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      overflow-x: hidden;
    }

    main {
      flex: 1;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem 1.5rem;
    }

    .hero { max-width: 720px; text-align: center; }

    .logo {
      display: inline-flex;
      align-items: center;
      gap: 0.6rem;
      font-size: 1.05rem;
      font-weight: 600;
      letter-spacing: 0.02em;
      color: var(--text-dim);
      margin-bottom: 2.5rem;
    }
    .logo-mark {
      width: 36px; height: 36px;
      background: linear-gradient(135deg, var(--primary), #8b5cf6);
      border-radius: 9px;
      display: grid; place-items: center;
      font-weight: 800; font-size: 1.05rem; color: white;
    }

    h1 {
      font-size: clamp(2.2rem, 6vw, 3.6rem);
      font-weight: 800;
      letter-spacing: -0.025em;
      line-height: 1.1;
      margin-bottom: 1.2rem;
      background: linear-gradient(180deg, #ffffff 0%, #cbd5e1 100%);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
    }

    .tagline {
      font-size: clamp(1rem, 2.2vw, 1.15rem);
      color: var(--text-dim);
      max-width: 520px;
      margin: 0 auto 2.4rem;
      line-height: 1.55;
    }

    .cta {
      display: inline-flex;
      align-items: center;
      gap: 0.55rem;
      background: var(--primary);
      color: white;
      border: none;
      padding: 0.95rem 1.6rem;
      font-size: 1rem;
      font-weight: 600;
      border-radius: 10px;
      cursor: pointer;
      transition: background 160ms ease, transform 160ms ease;
      box-shadow: 0 10px 30px rgba(59, 130, 246, 0.25);
    }
    .cta:hover { background: var(--primary-hover); transform: translateY(-1px); }
    .cta:active { transform: translateY(0); }

    footer {
      text-align: center;
      padding: 1.5rem;
      color: var(--text-dim);
      font-size: 0.85rem;
      border-top: 1px solid var(--border);
    }

    /* Overlay */
    .overlay {
      position: fixed; inset: 0;
      background: rgba(7, 11, 22, 0.7);
      backdrop-filter: blur(8px);
      -webkit-backdrop-filter: blur(8px);
      display: none;
      align-items: center;
      justify-content: center;
      padding: 1.5rem;
      z-index: 50;
      animation: fadeIn 200ms ease;
    }
    .overlay.open { display: flex; }

    .modal {
      background: linear-gradient(180deg, #131a2b 0%, #0e1422 100%);
      border: 1px solid var(--border);
      border-radius: 18px;
      padding: 2rem;
      width: 100%;
      max-width: 720px;
      box-shadow: 0 30px 80px rgba(0, 0, 0, 0.5);
      animation: slideUp 240ms ease;
    }

    .modal-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 0.4rem;
    }
    .modal-title { font-size: 1.25rem; font-weight: 700; }
    .modal-close {
      background: none; border: none;
      color: var(--text-dim);
      font-size: 1.6rem;
      cursor: pointer;
      line-height: 1;
      padding: 0.2rem 0.5rem;
      border-radius: 6px;
      transition: background 120ms;
    }
    .modal-close:hover { background: rgba(255,255,255,0.06); color: var(--text); }
    .modal-subtitle { color: var(--text-dim); font-size: 0.95rem; margin-bottom: 1.5rem; }

    .grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 0.85rem;
    }
    @media (max-width: 520px) { .grid { grid-template-columns: 1fr; } }

    .card {
      display: flex;
      align-items: center;
      gap: 0.9rem;
      background: var(--card-bg);
      border: 1px solid var(--border);
      border-radius: 12px;
      padding: 1rem 1.1rem;
      color: var(--text);
      text-decoration: none;
      transition: all 160ms ease;
      cursor: pointer;
    }
    .card:hover {
      background: var(--card-bg-hover);
      border-color: rgba(59, 130, 246, 0.4);
      transform: translateY(-2px);
    }
    .card-icon {
      flex-shrink: 0;
      width: 42px; height: 42px;
      border-radius: 10px;
      display: grid; place-items: center;
      font-size: 1.2rem;
    }
    .icon-client { background: rgba(59, 130, 246, 0.15); color: #60a5fa; }
    .icon-partner { background: rgba(168, 85, 247, 0.15); color: #c084fc; }
    .icon-driver { background: rgba(34, 197, 94, 0.15); color: #4ade80; }
    .icon-admin { background: rgba(244, 114, 182, 0.15); color: #f472b6; }

    .card-text { display: flex; flex-direction: column; gap: 0.15rem; min-width: 0; }
    .card-label { font-weight: 600; font-size: 0.98rem; }
    .card-domain { font-size: 0.8rem; color: var(--text-dim); font-family: ui-monospace, "SF Mono", Menlo, monospace; }

    @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
    @keyframes slideUp { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }
  </style>
</head>
<body>
  <main>
    <section class="hero">
      <div class="logo">
        <span class="logo-mark">F</span>
        <span>FlotteQ</span>
      </div>
      <h1>Pilotez votre flotte<br />en toute simplicité.</h1>
      <p class="tagline">
        La plateforme tout-en-un pour gérer véhicules, conducteurs, partenaires
        et opérations — pensée pour les flottes professionnelles.
      </p>
      <button class="cta" id="loginBtn" type="button" aria-haspopup="dialog">
        Connexion
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 12h14M13 5l7 7-7 7"/></svg>
      </button>
    </section>
  </main>

  <footer>© <span id="year"></span> FlotteQ. Tous droits réservés.</footer>

  <div class="overlay" id="overlay" role="dialog" aria-modal="true" aria-labelledby="modalTitle" aria-hidden="true">
    <div class="modal">
      <div class="modal-header">
        <h2 class="modal-title" id="modalTitle">Choisissez votre espace</h2>
        <button class="modal-close" id="closeBtn" aria-label="Fermer">×</button>
      </div>
      <p class="modal-subtitle">Sélectionnez le portail correspondant à votre profil.</p>

      <div class="grid">
        <a class="card" href="https://app.flotteq.fr">
          <div class="card-icon icon-client" aria-hidden="true">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg>
          </div>
          <div class="card-text">
            <span class="card-label">Espace Client</span>
            <span class="card-domain">app.flotteq.fr</span>
          </div>
        </a>

        <a class="card" href="https://partner.flotteq.fr">
          <div class="card-icon icon-partner" aria-hidden="true">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"/></svg>
          </div>
          <div class="card-text">
            <span class="card-label">Espace Partenaire</span>
            <span class="card-domain">partner.flotteq.fr</span>
          </div>
        </a>

        <a class="card" href="https://driver.flotteq.fr">
          <div class="card-icon icon-driver" aria-hidden="true">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M14 16H9m10 0h2.5a.5.5 0 0 0 .5-.5v-3.13a3 3 0 0 0-1.78-2.74l-3.4-1.51a2 2 0 0 1-1.05-1.07L14.79 4.7a2 2 0 0 0-1.81-1.2H6.5a2 2 0 0 0-2 2V16h2"/><circle cx="7" cy="17" r="2"/><circle cx="17" cy="17" r="2"/></svg>
          </div>
          <div class="card-text">
            <span class="card-label">Espace Conducteur</span>
            <span class="card-domain">driver.flotteq.fr</span>
          </div>
        </a>

        <a class="card" href="https://admin.flotteq.fr">
          <div class="card-icon icon-admin" aria-hidden="true">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
          </div>
          <div class="card-text">
            <span class="card-label">Administration</span>
            <span class="card-domain">admin.flotteq.fr</span>
          </div>
        </a>
      </div>
    </div>
  </div>

  <script>
    document.getElementById('year').textContent = new Date().getFullYear();
    const overlay = document.getElementById('overlay');
    const open = () => { overlay.classList.add('open'); overlay.setAttribute('aria-hidden', 'false'); };
    const close = () => { overlay.classList.remove('open'); overlay.setAttribute('aria-hidden', 'true'); };
    document.getElementById('loginBtn').addEventListener('click', open);
    document.getElementById('closeBtn').addEventListener('click', close);
    overlay.addEventListener('click', (e) => { if (e.target === overlay) close(); });
    document.addEventListener('keydown', (e) => { if (e.key === 'Escape' && overlay.classList.contains('open')) close(); });
  </script>
</body>
</html>
